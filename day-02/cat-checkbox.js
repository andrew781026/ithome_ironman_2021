class CatCheckbox extends HTMLElement  {
  constructor() {
    // Always call super first in constructor
    super();

    // Element functionality written in here

    // image & insect
  }
}

customElements.define('cat-checkbox', CatCheckbox);
